rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /opme/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
    match /coletas/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
